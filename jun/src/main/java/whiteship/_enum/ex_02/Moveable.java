package whiteship._enum.ex_02;

public interface Moveable {
    String moveSouth(Direction direction);
    String moveNorth(Direction direction);
    String moveWest(Direction direction);
    String moveEast(Direction direction);
}
